@extends('layouts.app')

@section('content')
<section class="hero">
    <div class="container hero-grid">
        <div>
            <p class="eyebrow">Open source · JDK 21</p>
            <h1>The Kotlin Framework<br><em>for Web Artisans</em></h1>
            <p class="lede">Kotlimo is a web application framework with expressive, elegant syntax. It gives Kotlin an IoC container, routing, Blade-like views, an Eloquent-style ORM, sessions, CSRF, auth, validation, queues, mail, and a Craft CLI.</p>
            <div class="hero-actions">
                <div class="command" data-copy="./gradlew :website:run">
                    <span class="prompt">$</span>
                    <code>./gradlew :website:run</code>
                    <button type="button" class="copy">Copy</button>
                </div>
                <a class="btn btn-lg" href="/docs/installation">Get started</a>
            </div>
        </div>
        <aside class="editor" aria-label="Kotlimo code example">
            <div class="editor-chrome">
                <span></span><span></span><span></span>
                <strong>routes/web.kt</strong>
            </div>
            <pre><code><span class="kw">Route</span>.get(<span class="str">"/users/{id}"</span>) { request ->
    val user = <span class="kw">User</span>.findOrFail(request.route(<span class="str">"id"</span>))

    <span class="kw">view</span>(<span class="str">"users.show"</span>, mapOf(
        <span class="str">"user"</span> to user
    ))
}</code></pre>
        </aside>
    </div>
</section>

<section class="feature">
    <div class="container feature-grid">
        <div>
            <h2>A progressive framework</h2>
            <p>Kotlimo grows with you. Start with routing, views, and a service container. When you are ready, reach for Eloquent-style models, validation, migrations, caching, events, and the Craft CLI — without leaving the happy path.</p>
            <ul class="checks">
                <li>Expressive routing and middleware</li>
                <li>Constructor auto-wiring IoC container</li>
                <li>Query builder and Active Record models</li>
                <li>Sessions, CSRF, and session-based auth</li>
                <li>Kote templates with Blade-like syntax</li>
            </ul>
        </div>
        <div class="panel">
            <pre><code><span class="kw">class</span> <span class="type">UserController</span> {
    <span class="kw">fun</span> store(request: <span class="type">Request</span>): <span class="type">Response</span> {
        val data = request.validate(mapOf(
            <span class="str">"name"</span> to <span class="str">"required|string|max:255"</span>,
            <span class="str">"email"</span> to <span class="str">"required|email"</span>
        ))

        val user = <span class="kw">User</span>.create(data)

        <span class="kw">return</span> redirect(<span class="str">"/users/"</span> + user.id)
    }
}</code></pre>
        </div>
    </div>
</section>

<section class="feature feature-alt">
    <div class="container feature-grid reverse">
        <div class="panel">
            <pre><code><span class="kw">class</span> <span class="type">User</span> : <span class="type">Model</span>() {
    <span class="kw">override val</span> fillable = listOf(<span class="str">"name"</span>, <span class="str">"email"</span>)

    <span class="kw">fun</span> posts() = hasMany(<span class="type">Post</span>::class)

    <span class="kw">companion object</span> : <span class="type">ModelCompanion</span>&lt;<span class="type">User</span>&gt;(<span class="type">User</span>::class)
}

val user = <span class="kw">User</span>.find(1)
val posts = user?.hasMany(<span class="type">Post</span>::class)</code></pre>
        </div>
        <div>
            <h2>Eloquent-style database</h2>
            <p>Talk to your database with a fluent query builder, or map tables to models with Active Record helpers, relationships, migrations, and schema blueprints. Tests run against H2 or SQLite in milliseconds.</p>
            <a class="text-link" href="/docs/database">Explore the ORM →</a>
        </div>
    </div>
</section>

<section class="kits">
    <div class="container">
        <h2>What ships in the repo</h2>
        <p class="section-copy">Kotlimo is a Gradle multi-module project. This documentation site is itself a Kotlimo application in <code class="inline">website/</code>.</p>
        <div class="card-grid">
            <article class="card">
                <h3>HTTP &amp; routing</h3>
                <p>Verbs, path parameters, named routes, groups, middleware, and an HTTP kernel.</p>
                <a href="/docs/routing">Routing docs</a>
            </article>
            <article class="card">
                <h3>Kote views</h3>
                <p>Layouts, sections, includes, and Blade-like syntax compiled to escaped HTML.</p>
                <a href="/docs/views">View docs</a>
            </article>
            <article class="card">
                <h3>Craft CLI</h3>
                <p>Scaffold an app, migrate, generate classes, serve, or export this site.</p>
                <a href="/docs/artisan">CLI docs</a>
            </article>
        </div>
    </div>
</section>
@endsection
